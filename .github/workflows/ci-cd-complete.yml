name: Complete CI/CD Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install flake8 black pytest pytest-cov pytest-html

  quality:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Code quality checks
        run: |
          flake8 app/ tests/ --max-line-length=88 --extend-ignore=E402
          black --check .

  tests:
    needs: build
    runs-on: ubuntu-latest
    env:
      APP_ENV: testing
      DB_URL: ${{ secrets.DB_URL }}
      API_KEY: ${{ secrets.API_KEY }}
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Run tests with coverage
        run: |
          python -m pytest --cov=app --cov-report=xml:coverage.xml --html=report.html
      
      - name: Upload test reports
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            coverage.xml
            report.html

  deploy:
    needs: [quality, tests]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && success()
    
    steps:
      - name: Deploy to production
        run: |
          echo "🚀 Déploiement en production..."
          echo "Application déployée avec succès !"
          echo "Commit: ${{ github.sha }}"
      
      - name: Simulation webhook
        run: |
          echo "Webhook de déploiement déclenché"
          echo "URL: ${{ secrets.DEPLOY_HOOK }}"